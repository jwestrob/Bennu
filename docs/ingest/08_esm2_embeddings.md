# Stage 08 — ESM2 Embeddings

Per‑protein embeddings and LanceDB indexes for vector search.

Entry: `src/ingest/06_esm2_embeddings.py`

Notes:
- Adjustable batch size via CLI `--threads` (router uses `threads//2` for batch size).
- Artifacts under `data/stage08_esm2/`.

