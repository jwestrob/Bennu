# Stage 02 — DFAST_QC (Optional)

ANI/CheckM taxonomy and quality metrics when enabled (`--skip-tax` disables).

Entry: `src/ingest/02_dfast_qc.py`

Outputs: per‑genome taxonomy/quality JSONs under `data/stage02_dfast_qc/`.

